View(predraw)
gc()
library(dplyr)
# import and read the MechaCar_mpg file
mechaCar_df <- read.csv(file='MechCar_mpg.csv',check.names=F,stringsAsFactors = F)
# import and read the MechaCar_mpg file
mechaCar_df <- read.csv(file='MechaCar_mpg.csv',check.names=F,stringsAsFactors = F)
# Perform mutiple linear regression on 6 variables
lm(mpg ~ vehicle_length + vehicle_weight + spoiler_angle + ground_clearance + AWD, data=mechaCar_df)
# Perform multiple linear regression on 6 variables
summary(lm(mpg ~ vehicle_length + vehicle_weight + spoiler_angle + ground_clearance + AWD, data=mechaCar_df))
# Perform multiple linear regression on 6 variables
lm(mpg ~ vehicle_length + vehicle_weight + spoiler_angle + ground_clearance + AWD, data=mechaCar_df)
# Generate summary statistics
summary(lm(mpg ~ vehicle_length + vehicle_weight + spoiler_angle + ground_clearance + AWD, data=mechaCar_df))
setwd("C:/Users/HP/Desktop/Data Analysis & Visualization Boot Camp/Module 15 - Statistics & R/Module 15 Challenge/MechaCar_Statistical_Analysis")
# import and read the MechaCar_mpg file
mechaCar_df <- read.csv(file='Resources/MechaCar_mpg.csv',check.names=F,stringsAsFactors = F)
# Import and read the Suspension_Coil file
suspension_coil_df <- read.csv(file='Resources/Suspension_Coil.csv',check.names=F,stringsAsFactors = F)
# Create summary table for PSI column
total_summary <- suspension_coil_df %>%
summarize(Mean = mean(PSI), Median = median(PSI), Variance = var(PSI), SD = sd(PSI), .groups = 'keep')
total_summary
# Create lot_summary dataframe
lot_summary <- suspension_coil_df %>% group_by(Manufacturing_Lot) %>%
summarize(Mean = mean(PSI), Median = median(PSI), Variance = var(PSI), SD = sd(PSI), .groups = 'keep')
lot_summary
View(total_summary)
View(lot_summary)
# Perform t-test to compare the mean of the PSI across all manufacturing lots
# and the population mean
t.test(log10(sample_table$PSI),mu=mean(log10(suspension_coil_df$PSI)))
# create a sample table from the suspension coil population
sample_table <- suspension_coil_df %>% sample_n(50)
# Perform t-test to compare the mean of the PSI across all manufacturing lots
# and the population mean
t.test(log10(sample_table$PSI),mu=mean(log10(suspension_coil_df$PSI)))
# Perform t-test to compare the mean of the PSI across all manufacturing lots
# and the population mean
t.test(sample_table$PSI,mu=mean(suspension_coil_df$PSI))
?t.test()
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI,subset(sample_table$Manufacturing_Lot=="Lot1"),mu=mean(suspension_coil_df$PSI))
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI,subset=(sample_table$Manufacturing_Lot=="Lot1"),mu=mean(suspension_coil_df$PSI))
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI,subset=(sample_table$Manufacturing_Lot=="Lot2"),mu=mean(suspension_coil_df$PSI))
View(sample_table)
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI,subset(sample_table, Manufacturing_Lot=="Lot1"),mu=mean(suspension_coil_df$PSI))
sample_table$Manufacturing_Lot=="Lot1"
sample_table[sample_table$Manufacturing_Lot == "Lot1",]
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI,subset=sample_table[sample_table$Manufacturing_Lot == "Lot1",],mu=mean(suspension_coil_df$PSI))
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI,subset=sample_table[sample_table$Manufacturing_Lot == "Lot2",],mu=mean(suspension_coil_df$PSI))
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(PSI ~ , data=suspension_coil_df, subset=sample_table[sample_table$Manufacturing_Lot == "Lot2",])
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(PSI , data=suspension_coil_df, subset=sample_table[sample_table$Manufacturing_Lot == "Lot2",])
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(PSI ~ 1, data=suspension_coil_df, subset=sample_table[sample_table$Manufacturing_Lot == "Lot2",])
suspension_coil_df$PSI, suspension_coil_df$Manufacturing_Lot==1
[suspension_coil_df$PSI, suspension_coil_df$Manufacturing_Lot=="Lot1"]
suspension_coil_df$PSI, suspension_coil_df$Manufacturing_Lot=="Lot1"
suspension_coil_df$PSI[suspension_coil_df$Manufacturing_Lot=="Lot1"]
suspension_coil_df$VehicleID[suspension_coil_df$Manufacturing_Lot=="Lot1"]
View(suspension_coil_df)
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(PSI ~ 1, data=suspension_coil_df, subset=suspension_coil_df$PSI[suspension_coil_df$Manufacturing_Lot=="Lot1"])
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI, mu=1500, subset=sample_table$PSI[sample_table$Manufacturing_Lot=="Lot1"])
# Perform t-test to compare the mean of the PSI across all manufacturing lots
# and the population mean
t.test(sample_table$PSI,mu=1500)
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI, mu=1500, subset=sample_table$PSI[sample_table$Manufacturing_Lot=="Lot2"])
# Perform t-test to compare the mean of the PSI in lot 1
# and the population mean
t.test(sample_table$PSI, mu=1500, subset=sample_table$PSI[sample_table$Manufacturing_Lot=="Lot3"])
